# Backup-And-RestoreScripts

- The backup script will zip all files specified in the config file and create a backup directory.
- the restore script will unzip all zip files and create a restore directory
